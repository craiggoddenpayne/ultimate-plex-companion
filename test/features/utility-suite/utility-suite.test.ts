import test from 'node:test';
import assert from 'node:assert/strict';
import { utilitySuite } from '../../../src/server/features/utility-suite/utility-suite-server.ts';

test('Utility Matrix returns fifteen live analyzers',async()=>{const now=Math.floor(Date.now()/1000);const items=[{ratingKey:'1',type:'movie',title:'Arrival',year:2016,addedAt:now-1000,duration:7_000_000,viewOffset:2_000_000,audienceRating:8.1,Genre:[{tag:'Science Fiction'}],Media:[{id:'10',videoResolution:'4k',videoCodec:'hevc',videoDynamicRange:'HDR',Part:[{size:9_000_000_000}]}]},{ratingKey:'2',type:'movie',title:'Primer',year:2004,duration:4_600_000,audienceRating:7.5,Genre:[{tag:'Science Fiction'}],Media:[{id:'11',videoResolution:'1080',videoCodec:'h264',Part:[{size:4_000_000_000}]}]}];const report=await utilitySuite({}, {plexFetch:async(_config,path)=>path==='/library/sections'?{MediaContainer:{Directory:[{key:'1',title:'Movies',type:'movie'}]}}:{MediaContainer:{Metadata:[{ratingKey:'1',viewedAt:now-200*86400}]}},libraryItems:async()=>items},true);assert.equal(report.modules.length,15);assert.equal(report.modules.find(item=>item.id==='finish').metric,1);assert.equal(report.modules.find(item=>item.id==='showcase').metric,1);assert.equal(report.modules.find(item=>item.id==='codec').metric,1);assert.equal(report.modules.find(item=>item.id==='decades').items.length,2)});
