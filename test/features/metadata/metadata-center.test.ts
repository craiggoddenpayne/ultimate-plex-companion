import test from 'node:test';
import assert from 'node:assert/strict';
import { metadataCenter } from '../../../src/server/features/metadata/metadata-center-server.js';

test('metadata center detects missing and invalid Plex metadata',async()=>{const items=[{ratingKey:'1',type:'movie',title:'Film.1080p.x265',year:2099,summary:'Short',guid:'local://1',Genre:[]},{ratingKey:'2',type:'movie',title:'Healthy Film',year:2020,summary:'A complete and sufficiently descriptive summary for this well matched film.',thumb:'/thumb',guid:'tmdb://2',Genre:[{tag:'Drama'}]}];const plexFetch=async(_config,path)=>path==='/library/sections'?{MediaContainer:{Directory:[{key:'7',title:'Movies',type:'movie'}]}}:{MediaContainer:{totalSize:2}};const data=await metadataCenter({}, {plexFetch,libraryItems:async()=>items},true);assert.equal(data.scanned,2);assert.equal(data.issueCount,1);assert.equal(data.missing,1);assert.equal(data.invalid,1);assert.ok(data.issues[0].problems.some(item=>item.code==='missing-artwork'));assert.ok(data.issues[0].problems.some(item=>item.code==='invalid-year'));assert.ok(data.categories.length>=4)});
